name: geo_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3
  - geopandas
  - jupyter
  - geodatasets
  - plotly
  - rasterio
  - osmnx
  - contextily
prefix: /opt/miniconda3/envs/geo_env
